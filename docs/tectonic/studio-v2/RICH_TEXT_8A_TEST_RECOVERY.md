# Rich Text 8A — test recovery

This pack restores only the canonical `tectonic/studio-v2/test_rich_text_8a.js`
for the current 8A implementation (`studioInline*`, `data-inline-command`,
`inlineRichText`).

It does not modify product code.
